---
phase: 02-media-manifest-qa
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - package.json
  - src/shared/preflight-qa.ts
  - src/shared/preflight-qa.test.ts
  - scripts/preflight-media.ts
  - src/server/services/renderer.ts
  - src/server/routes/render.ts
autonomous: true
requirements:
  - MEDIA-02
  - MEDIA-03
user_setup: []
must_haves:
  truths:
    - render 前会自动执行媒体一致性检查，而不是继续依赖人工确认
    - 音频、字幕、scene durations 与 total frames 不一致时会被硬阻断
    - 报错会明确指出具体 scene、文件或字段，而不是只给通用失败提示
  artifacts:
    - src/shared/preflight-qa.ts
    - src/shared/preflight-qa.test.ts
    - scripts/preflight-media.ts
    - src/server/services/renderer.ts
    - src/server/routes/render.ts
  key_links:
    - src/shared/preflight-qa.ts -> src/shared/media-manifest.ts
    - scripts/preflight-media.ts -> src/shared/preflight-qa.ts
    - src/server/services/renderer.ts -> src/shared/preflight-qa.ts
    - src/server/routes/render.ts -> src/server/services/renderer.ts
---

<objective>
把 media manifest 真正变成 render 前的 QA 门禁：新增共享 preflight 校验模块、CLI 检查命令，以及 API render 阻断逻辑。

Purpose: 让媒体数据漂移在渲染前暴露，而不是继续产出错误成片后再返工。
Output: `preflight-qa` 共享模块、`media:check` 命令、renderer / route 的失败阻断接线，以及覆盖 issue shape 和失败路径的测试。
</objective>

<execution_context>
@/Users/qilin.guo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/qilin.guo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-media-manifest-qa/02-CONTEXT.md
@.planning/phases/02-media-manifest-qa/02-RESEARCH.md
@.planning/phases/02-media-manifest-qa/02-VALIDATION.md
@.planning/phases/02-media-manifest-qa/02-01-PLAN.md
@src/shared/media-manifest.ts
@src/server/services/renderer.ts
@src/server/routes/render.ts
@src/shared/render-contract.ts
@src/compositions/catalog.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义共享 preflight QA issue schema 与校验规则</name>
  <files>src/shared/preflight-qa.ts, src/shared/preflight-qa.test.ts</files>
  <read_first>src/shared/media-manifest.ts, src/compositions/catalog.ts, src/shared/render-contract.ts, .planning/phases/02-media-manifest-qa/02-RESEARCH.md</read_first>
  <action>新增 `src/shared/preflight-qa.ts`，导出至少这些实体：`PreflightIssueSchema`、`PreflightReportSchema`、`runMediaPreflight()`。issue 字段必须显式包含 `severity`、`code`、`compositionId`、`sceneIndex`（可空）、`field`、`message`。`runMediaPreflight()` 至少执行 5 类检查：1) 每个 `audioFiles` 路径存在；2) `subtitleDataFile` 存在且 JSON 非空；3) `sceneDurations.length === sceneCount`；4) `totalDurationInFrames === sum(sceneDurations)`；5) `catalog.durationInFrames === manifest.totalDurationInFrames`。新增 `src/shared/preflight-qa.test.ts`，为上述每类 issue 各写一个可运行断言，测试文案里要直接出现 `missing-audio`、`missing-subtitle`、`scene-duration-mismatch`、`total-duration-mismatch`、`catalog-duration-mismatch` 这类固定 code。</action>
  <verify>npx vitest run src/shared/preflight-qa.test.ts</verify>
  <acceptance_criteria>
    - `src/shared/preflight-qa.ts` contains `export const PreflightIssueSchema`
    - `src/shared/preflight-qa.ts` contains `runMediaPreflight`
    - `src/shared/preflight-qa.test.ts` contains `missing-audio`
    - `src/shared/preflight-qa.test.ts` contains `catalog-duration-mismatch`
  </acceptance_criteria>
  <done>QA 规则成为共享合同，CLI 与 API 不再各自实现一套检查逻辑。</done>
</task>

<task type="auto">
  <name>Task 2: 增加统一 preflight 命令并输出定位式结果</name>
  <files>package.json, scripts/preflight-media.ts</files>
  <read_first>package.json, src/shared/preflight-qa.ts, src/shared/media-manifest.ts</read_first>
  <action>新增 `scripts/preflight-media.ts`，实现通用 CLI：读取 `--id <compositionId>` 或 `--manifest <path>`，运行 `runMediaPreflight()`，当无问题时打印 `PRECHECK_OK` 和 composition id；有问题时逐条打印 `[{code}] scene=<index> field=<field> message=<message>`，并以非零退出码结束。在 `package.json` 中增加 `"media:check": "npx tsx scripts/preflight-media.ts"` 与 `"media:check:aiharnessengineer": "npm run media:check -- --id AIHarnessEngineer"`。不要做静默 warning；有 issue 时命令必须 fail。</action>
  <verify>npm run media:check:aiharnessengineer</verify>
  <acceptance_criteria>
    - `package.json` contains `"media:check": "npx tsx scripts/preflight-media.ts"`
    - `package.json` contains `"media:check:aiharnessengineer"`
    - `scripts/preflight-media.ts` contains `PRECHECK_OK`
    - `scripts/preflight-media.ts` contains `process.exit(1)`
  </acceptance_criteria>
  <done>创作者可以在 render 前独立跑一次硬检查，快速定位 manifest 漂移或缺资源问题。</done>
</task>

<task type="auto">
  <name>Task 3: 把 preflight QA 接进 renderer 和 render route 的失败阻断</name>
  <files>src/server/services/renderer.ts, src/server/routes/render.ts</files>
  <read_first>src/server/services/renderer.ts, src/server/routes/render.ts, src/shared/preflight-qa.ts, src/shared/render-contract.ts</read_first>
  <action>更新 `src/server/services/renderer.ts`，在 `renderMedia()` 之前引入 `runMediaPreflight()`：按 `request.compositionId` 解析默认 manifest 路径 `src/data/<slug>-media-manifest.json`，若 report 中存在 error 级 issue，则直接返回 `{ success: false, error: "...具体 issue 摘要..." }`，不得继续调用 `renderMedia()`。同时更新 `src/server/routes/render.ts`，保留现有 400/500 shape，但当 renderer 返回 preflight 失败时，响应 JSON 必须带上 issue code 或 scene/field 信息，避免只返回 `Render failed`。这里不要把 QA 藏到日志里；失败信息必须进入响应体。</action>
  <verify>npm run typecheck && npx vitest run src/shared/preflight-qa.test.ts</verify>
  <acceptance_criteria>
    - `src/server/services/renderer.ts` contains `runMediaPreflight`
    - `src/server/services/renderer.ts` contains `src/data/`
    - `src/server/routes/render.ts` contains `error`
    - `src/server/routes/render.ts` contains `details`
  </acceptance_criteria>
  <done>render 入口在媒体数据不一致时会被明确阻断，错误能定位到具体问题源。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npx vitest run src/shared/preflight-qa.test.ts`
- [ ] `npm run media:check:aiharnessengineer`
- [ ] 制造一个 manifest / catalog duration mismatch，确认 render 在调用 `renderMedia()` 前失败
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `MEDIA-02` 的一致性检查在 CLI 与 API 路径上共享同一逻辑
- `MEDIA-03` 的失败信息可定位到 scene、field 或具体文件
- Phase 2 完成后，错误媒体数据不会再默默进入正式 render
  </success_criteria>

<output>
After completion, create `.planning/phases/02-media-manifest-qa/02-02-SUMMARY.md`
</output>
