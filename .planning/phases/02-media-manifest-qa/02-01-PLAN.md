---
phase: 02-media-manifest-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/shared/media-manifest.ts
  - src/shared/media-manifest.test.ts
  - scripts/build-media-manifest.ts
autonomous: true
requirements:
  - MEDIA-01
user_setup: []
must_haves:
  truths:
    - 创作者可以通过统一命令生成 voiceover、字幕、scene durations 与 total duration 的 machine-readable manifest
    - manifest 同时记录媒体输入依赖和输出结果，而不是只在终端打印数字
    - 新旧 topic 都可以复用同一份 manifest 生成合同，不再每条视频一套 shape
  artifacts:
    - package.json
    - src/shared/media-manifest.ts
    - src/shared/media-manifest.test.ts
    - scripts/build-media-manifest.ts
  key_links:
    - scripts/build-media-manifest.ts -> src/shared/media-manifest.ts
    - src/shared/media-manifest.ts -> src/shared/video-registry.ts
    - src/shared/media-manifest.ts -> src/compositions/catalog.ts
    - src/shared/media-manifest.test.ts -> src/shared/media-manifest.ts
---

<objective>
建立 Phase 2 的 media manifest 真相源：用一个共享 schema 和一个统一 builder 命令，把 scene audio、subtitle、scene durations 与 total frames 收敛为可校验的 JSON 产物。

Purpose: 先把“媒体时序真相”写成文件，而不是继续让 sync 脚本只在终端输出数字、再人工抄回 `Root.tsx`。
Output: `MediaManifestSchema`、通用 `build-media-manifest` 脚本、npm 命令入口，以及至少一个现有视频可生成的 manifest 文件。
</objective>

<execution_context>
@/Users/qilin.guo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/qilin.guo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-media-manifest-qa/02-CONTEXT.md
@.planning/phases/02-media-manifest-qa/02-RESEARCH.md
@.planning/phases/02-media-manifest-qa/02-VALIDATION.md
@package.json
@src/shared/video-registry.ts
@src/compositions/catalog.ts
@scripts/sync-subtitle.ts
@scripts/sync-subtitle-aiharnessengineer.ts
@src/compositions/AIHarnessEngineer/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义共享 media manifest schema 与 builder helper</name>
  <files>src/shared/media-manifest.ts</files>
  <read_first>src/shared/video-registry.ts, src/compositions/catalog.ts, .planning/phases/02-media-manifest-qa/02-CONTEXT.md, .planning/phases/02-media-manifest-qa/02-RESEARCH.md</read_first>
  <action>新增 `src/shared/media-manifest.ts`，用 Zod 定义至少这些导出：`SceneMediaArtifactSchema`、`MediaManifestSchema`、`MediaManifestIssueSchema`、`buildMediaManifest()`。`MediaManifestSchema` 必须显式包含 `compositionId`、`stillId`、`sceneCount`、`sceneDurations`、`totalDurationInFrames`、`audioFiles`、`subtitleDataFile`、`output.video`、`output.cover`。`buildMediaManifest()` 的输入必须至少要求 `compositionId`、`audioInfos`、`sceneDurations`、`subtitleDataFile`，并从 `video-registry` / `catalog` 补出 `stillId` 与输出路径。不要把视觉主题、scene cards 或 hook props 放进 manifest；Phase 2 只负责媒体时序真相。</action>
  <verify>npx vitest run src/shared/media-manifest.test.ts</verify>
  <acceptance_criteria>
    - `src/shared/media-manifest.ts` contains `export const MediaManifestSchema`
    - `src/shared/media-manifest.ts` contains `totalDurationInFrames`
    - `src/shared/media-manifest.ts` contains `subtitleDataFile`
    - `src/shared/media-manifest.ts` contains `buildMediaManifest`
  </acceptance_criteria>
  <done>manifest 具备可复用的 typed shape，后续 builder 与 preflight 不再各自猜字段。</done>
</task>

<task type="auto">
  <name>Task 2: 增加统一 manifest 生成命令并固定输出路径</name>
  <files>package.json, scripts/build-media-manifest.ts</files>
  <read_first>package.json, scripts/sync-subtitle-aiharnessengineer.ts, src/shared/media-manifest.ts, src/compositions/AIHarnessEngineer/schema.ts</read_first>
  <action>新增 `scripts/build-media-manifest.ts`，实现一个通用 CLI：读取 `--id <compositionId>`，用 `music-metadata` 读取 `public/audio/<slug>-sceneN.mp3` 的真实时长，读取 `src/data/<slug>-subtitles.json`，按现有同步规则计算 `sceneDurations` 与 `totalDurationInFrames`，最后写出 `src/data/<slug>-media-manifest.json`。在 `package.json` 中增加 `"media:build": "npx tsx scripts/build-media-manifest.ts"`，并至少补一个 wrapper：`"media:build:aiharnessengineer": "npm run media:build -- --id AIHarnessEngineer"`。命令执行成功时必须打印 manifest 输出路径和 `totalDurationInFrames`，失败时必须以非零退出码终止。</action>
  <verify>npm run media:build:aiharnessengineer</verify>
  <acceptance_criteria>
    - `package.json` contains `"media:build": "npx tsx scripts/build-media-manifest.ts"`
    - `package.json` contains `"media:build:aiharnessengineer"`
    - `scripts/build-media-manifest.ts` contains `--id`
    - `scripts/build-media-manifest.ts` contains `src/data/`
  </acceptance_criteria>
  <done>仓库具备统一 media manifest 生成入口，后续新增 topic 不需要再复制整套 sync 逻辑。</done>
</task>

<task type="auto">
  <name>Task 3: 用合同测试锁定 manifest 输出行为</name>
  <files>src/shared/media-manifest.test.ts</files>
  <read_first>src/shared/media-manifest.ts, src/shared/video-registry.ts, src/compositions/catalog.ts, .planning/REQUIREMENTS.md</read_first>
  <action>新增 `src/shared/media-manifest.test.ts`，至少覆盖 4 类行为：1) `buildMediaManifest()` 产物可以通过 `MediaManifestSchema`；2) `totalDurationInFrames` 等于 `sceneDurations` 求和；3) manifest 的 `output.video` 和 `stillId` 来自 registry / catalog，而不是输入方手填；4) 当 `sceneDurations.length` 与 `sceneCount` 不一致时会抛出错误。测试中至少用 `AIHarnessEngineer` 作为样本输入，并断言输出文件名为 `aiharnessengineer-media-manifest.json`。不要写快照；用明确字段断言。</action>
  <verify>npx vitest run src/shared/media-manifest.test.ts</verify>
  <acceptance_criteria>
    - `src/shared/media-manifest.test.ts` contains `describe("MediaManifestSchema"`
    - `src/shared/media-manifest.test.ts` contains `totalDurationInFrames`
    - `src/shared/media-manifest.test.ts` contains `AIHarnessEngineer`
    - `src/shared/media-manifest.test.ts` contains `aiharnessengineer-media-manifest.json`
  </acceptance_criteria>
  <done>manifest 生成逻辑有自动保护，后续调整 buffer / 文件命名或 registry 映射时能快速发现回归。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npx vitest run src/shared/media-manifest.test.ts`
- [ ] `npm run media:build:aiharnessengineer`
- [ ] `src/data/aiharnessengineer-media-manifest.json` 存在，且包含 `sceneDurations` 与 `totalDurationInFrames`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `MEDIA-01` 对应的媒体真相不再只存在于终端日志和人工复制步骤里
- 统一命令能生成 machine-readable manifest，而不是每条视频各自输出不同 shape
- Phase 2 的 preflight QA 可以直接复用 manifest 文件，不需重新读取一堆 topic-specific 规则
  </success_criteria>

<output>
After completion, create `.planning/phases/02-media-manifest-qa/02-01-SUMMARY.md`
</output>
